<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.kokuwa</groupId>
		<artifactId>maven-parent</artifactId>
		<version>0.4.0</version>
		<relativePath />
	</parent>

	<groupId>io.inoa.fleet</groupId>
	<artifactId>gateway-registry</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>GatewayRegistry</name>
	<url>https://github.com/kokuwaio/gateway-registry</url>
	<inceptionYear>2020</inceptionYear>
	<licenses>
		<license>
			<name>Apache License 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0</url>
		</license>
	</licenses>

	<modules>
		<module>service</module>
		<module>hono-bridge</module>
		<module>test</module>
	</modules>

	<scm>
		<url>https://github.com/grayc-de/gateway-registry</url>
		<connection>scm:git:https://github.com/grayc-de/gateway-registry.git</connection>
		<developerConnection>scm:git:https://github.com/grayc-de/gateway-registry.git</developerConnection>
		<tag>HEAD</tag>
	</scm>
	<issueManagement>
		<system>jira</system>
		<url>https://grayc.atlassian.net/browse/INOA</url>
	</issueManagement>

	<properties>

		<!-- ===================================================================== -->
		<!-- =============================== Build =============================== -->
		<!-- ===================================================================== -->

		<docker.postgres.version>13.1</docker.postgres.version>
		<docker.image.repository>ghcr.io/grayc-de</docker.image.repository>
		<docker.image.version>latest</docker.image.version>

		<!-- ===================================================================== -->
		<!-- ============================== Libaries ============================= -->
		<!-- ===================================================================== -->

		<version.com.github.tomakehurst.wiremock>2.27.2</version.com.github.tomakehurst.wiremock>
		<version.io.kokuwa.micronaut.codegen>2.1.8</version.io.kokuwa.micronaut.codegen>
		<version.io.kokuwa.micronaut.logging>0.0.4</version.io.kokuwa.micronaut.logging>
		<version.io.micronaut>2.5.12</version.io.micronaut>
		<version.io.micronaut.data>2.5.0</version.io.micronaut.data>
		<version.org.awaitility>4.1.0</version.org.awaitility>
		<version.org.eclipse.hono>1.9.0</version.org.eclipse.hono>
		<version.org.keycloak>15.0.2</version.org.keycloak>
		<version.org.mapstruct>1.5.0.Beta1</version.org.mapstruct>
		<version.org.springframework.boot>2.4.8</version.org.springframework.boot>
		<version.org.testcontainers>1.16.0</version.org.testcontainers>

	</properties>

	<dependencyManagement>
		<dependencies>

			<!-- micronaut -->
			<dependency>
				<groupId>io.micronaut</groupId>
				<artifactId>micronaut-bom</artifactId>
				<version>${version.io.micronaut}</version>
			</dependency>
			<dependency>
				<groupId>io.micronaut.data</groupId>
				<artifactId>micronaut-data-processor</artifactId>
				<version>${version.io.micronaut.data}</version>
			</dependency>
			<dependency>
				<groupId>io.kokuwa.micronaut</groupId>
				<artifactId>micronaut-logging</artifactId>
				<version>${version.io.kokuwa.micronaut.logging}</version>
			</dependency>

			<!-- spring -->
			<dependency>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-dependencies</artifactId>
				<version>${version.org.springframework.boot}</version>
			</dependency>

			<!-- libraries -->
			<dependency>
				<groupId>com.github.tomakehurst</groupId>
				<artifactId>wiremock-jre8-standalone</artifactId>
				<version>${version.com.github.tomakehurst.wiremock}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.hono</groupId>
				<artifactId>hono-bom</artifactId>
				<version>${version.org.eclipse.hono}</version>
			</dependency>
			<dependency>
				<groupId>org.keycloak.bom</groupId>
				<artifactId>keycloak-bom-parent</artifactId>
				<version>${version.org.keycloak}</version>
			</dependency>
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct</artifactId>
				<version>${version.org.mapstruct}</version>
			</dependency>
			<dependency>
				<groupId>org.testcontainers</groupId>
				<artifactId>testcontainers-bom</artifactId>
				<version>${version.org.testcontainers}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>org.awaitility</groupId>
				<artifactId>awaitility</artifactId>
				<version>${version.org.awaitility}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<build>
		<pluginManagement>
			<plugins>

				<!-- generate sources from openapi spec -->
				<plugin>
					<groupId>org.openapitools</groupId>
					<artifactId>openapi-generator-maven-plugin</artifactId>
					<configuration>
						<generatorName>micronaut</generatorName>
						<modelNameSuffix>VO</modelNameSuffix>
						<invokerPackage>io.inoa.fleet.registry.rest</invokerPackage>
						<output>${project.build.directory}</output>
						<configOptions>
							<useLombokGenerated>true</useLombokGenerated>
							<dateTimeRelaxed>false</dateTimeRelaxed>
							<visitable>false</visitable>
						</configOptions>
						<typeMappings>
							<typeMapping>Nullable=io.micronaut.core.annotation.Nullable</typeMapping>
							<typeMapping>Nonnull=io.micronaut.core.annotation.NonNull</typeMapping>
						</typeMappings>
						<ignoreFileOverride>${project.basedir}/../.openapi-ignore</ignoreFileOverride>
					</configuration>
					<dependencies>
						<dependency>
							<groupId>io.kokuwa.micronaut</groupId>
							<artifactId>micronaut-openapi-codegen</artifactId>
							<version>${version.io.kokuwa.micronaut.codegen}</version>
						</dependency>
					</dependencies>
				</plugin>

				<!-- adjust jacoco excludes -->
				<plugin>
					<groupId>org.jacoco</groupId>
					<artifactId>jacoco-maven-plugin</artifactId>
					<configuration>
						<excludes>
							<exclude>**/*VO.class</exclude>
							<exclude>**/*MapperImpl.class</exclude>
						</excludes>
					</configuration>
				</plugin>

				<!-- create container -->
				<plugin>
					<groupId>com.google.cloud.tools</groupId>
					<artifactId>jib-maven-plugin</artifactId>
					<configuration>
						<container>
							<jvmFlags>
								<jvmFlag>-XX:+ExitOnOutOfMemoryError</jvmFlag>
							</jvmFlags>
						</container>
						<to>
							<image>${docker.image.repository}/${project.artifactId}:${docker.image.version}</image>
						</to>
					</configuration>
				</plugin>

			</plugins>
		</pluginManagement>
		<plugins>

			<!-- do not deploy anything -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<executions>
					<execution>
						<id>default-deploy</id>
						<phase />
					</execution>
				</executions>
			</plugin>

		</plugins>
	</build>
</project>
