<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>io.kokuwa.maven</groupId>
		<artifactId>maven-parent</artifactId>
		<version>0.6.14</version>
		<relativePath/>
	</parent>

	<groupId>io.inoa</groupId>
	<artifactId>inoa-parent</artifactId>
	<version>0.0.1-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>INOA</name>
	<url>https://docs.inoa.io</url>
	<inceptionYear>2021</inceptionYear>
	<organization>
		<name>INOA</name>
		<url>https://git.grayc.io/inoa/cloud</url>
	</organization>
	<licenses>
		<license>
			<name>Apache-2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<modules>
		<module>api</module>
		<module>service</module>
		<module>app</module>
		<module>image</module>
		<module>test</module>
	</modules>

	<scm>
		<url>https://git.grayc.io/inoa/cloud</url>
		<connection>scm:git:https://git.grayc.io/inoa/cloud.git</connection>
		<developerConnection>scm:git:https://git.grayc.io/inoa/cloud.git</developerConnection>
		<tag>HEAD</tag>
	</scm>
	<issueManagement>
		<system>gitea</system>
		<url>https://git.grayc.io/inoa/cloud/issues</url>
	</issueManagement>
	<ciManagement>
		<system>woodpecker</system>
		<url>https://ci.grayc.io/repos/15</url>
	</ciManagement>

	<properties>

		<!-- ===================================================================== -->
		<!-- ============================== Build ================================ -->
		<!-- ===================================================================== -->

		<project.build.outputTimestamp>2024-01-10T14:04:49Z</project.build.outputTimestamp>

		<maven.compiler.source>17</maven.compiler.source>
		<maven.compiler.target>17</maven.compiler.target>
		<maven.compiler.failOnWarning>false</maven.compiler.failOnWarning>

		<maven.release.preparationGoals>verify -N -P release-prepare</maven.release.preparationGoals>
		<maven.release.releaseProfiles>release-perform</maven.release.releaseProfiles>
		<maven.release.goals>deploy -Dcheck.skip -DskipTests -pl !test -T1C</maven.release.goals>
		<maven.release.scmReleaseCommitComment>@{prefix} prepare release @{releaseLabel} [CI SKIP]</maven.release.scmReleaseCommitComment>
		<maven.release.signTag>false</maven.release.signTag>

		<image.registry>ghcr.io/inoa-io</image.registry>
		<image.inoa.tag>snapshot</image.inoa.tag>
		<image.inoa>${image.registry}/inoa:${image.inoa.tag}</image.inoa>

		<!-- ===================================================================== -->
		<!-- ============================== Libaries ============================= -->
		<!-- ===================================================================== -->

		<version.com.influxdb.client>7.0.0</version.com.influxdb.client>
		<version.com.jayway.jsonpath>2.9.0</version.com.jayway.jsonpath>
		<version.io.cloudevents>2.5.0</version.io.cloudevents>
		<version.io.kokuwa.maven.k3s>1.2.4</version.io.kokuwa.maven.k3s>
		<version.io.kokuwa.micronaut.codegen>3.4.3</version.io.kokuwa.micronaut.codegen>
		<version.io.kokuwa.micronaut.influxdb>3.0.2</version.io.kokuwa.micronaut.influxdb>
		<version.io.kokuwa.micronaut.logging>3.3.0</version.io.kokuwa.micronaut.logging>
		<version.io.micronaut>3.10.3</version.io.micronaut>
		<version.io.micronaut.data>3.10.0</version.io.micronaut.data>
		<version.io.moquette>0.16</version.io.moquette>
		<version.org.apache.kafka>3.6.1</version.org.apache.kafka>
		<version.org.awaitility>4.2.0</version.org.awaitility>
		<version.org.bouncycastle>1.77</version.org.bouncycastle>
		<version.org.eclipse.paho>1.2.5</version.org.eclipse.paho>
		<version.org.keycloak>23.0.6</version.org.keycloak>
		<version.org.mapstruct>1.5.5.Final</version.org.mapstruct>
		<version.org.projectlombok>1.18.30</version.org.projectlombok>

		<version.io.traefik>v2.11.0</version.io.traefik>
		<version.org.postgresql>14.11-bullseye</version.org.postgresql>
		<version.com.influxdb>2.1.1-alpine</version.com.influxdb>

	</properties>

	<dependencyManagement>
		<dependencies>

			<!-- micronaut -->
			<dependency>
				<groupId>io.micronaut.data</groupId>
				<artifactId>micronaut-data-bom</artifactId>
				<version>${version.io.micronaut.data}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>io.micronaut</groupId>
				<artifactId>micronaut-bom</artifactId>
				<version>${version.io.micronaut}</version>
				<type>pom</type>
				<scope>import</scope>
			</dependency>
			<dependency>
				<groupId>io.micronaut</groupId>
				<artifactId>micronaut-inject</artifactId>
				<version>${version.io.micronaut}</version>
				<exclusions>
					<exclusion>
						<groupId>javax.annotation</groupId>
						<artifactId>javax.annotation-api</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>io.kokuwa.micronaut</groupId>
				<artifactId>micronaut-logging</artifactId>
				<version>${version.io.kokuwa.micronaut.logging}</version>
			</dependency>
			<dependency>
				<groupId>io.kokuwa.micronaut</groupId>
				<artifactId>micronaut-influxdb</artifactId>
				<version>${version.io.kokuwa.micronaut.influxdb}</version>
			</dependency>

			<!-- libraries -->
			<dependency>
				<groupId>com.influxdb</groupId>
				<artifactId>influxdb-client-reactive</artifactId>
				<version>${version.com.influxdb.client}</version>
				<exclusions>
					<exclusion>
						<groupId>com.google.code.findbugs</groupId>
						<artifactId>jsr305</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>com.jayway.jsonpath</groupId>
				<artifactId>json-path</artifactId>
				<version>${version.com.jayway.jsonpath}</version>
			</dependency>
			<dependency>
				<groupId>io.cloudevents</groupId>
				<artifactId>cloudevents-json-jackson</artifactId>
				<version>${version.io.cloudevents}</version>
				<exclusions>
					<exclusion>
						<groupId>com.fasterxml.jackson.core</groupId>
						<artifactId>jackson-core</artifactId>
					</exclusion>
					<exclusion>
						<groupId>com.fasterxml.jackson.core</groupId>
						<artifactId>jackson-databind</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>io.cloudevents</groupId>
				<artifactId>cloudevents-kafka</artifactId>
				<version>${version.io.cloudevents}</version>
				<exclusions>
					<exclusion>
						<groupId>org.apache.kafka</groupId>
						<artifactId>kafka-clients</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>io.moquette</groupId>
				<artifactId>moquette-broker</artifactId>
				<version>${version.io.moquette}</version>
				<exclusions>
					<exclusion>
						<groupId>com.bugsnag</groupId>
						<artifactId>bugsnag</artifactId>
					</exclusion>
					<exclusion>
						<groupId>org.slf4j</groupId>
						<artifactId>slf4j-log4j12</artifactId>
					</exclusion>
				</exclusions>
			</dependency>
			<dependency>
				<groupId>org.apache.kafka</groupId>
				<artifactId>kafka-clients</artifactId>
				<version>${version.org.apache.kafka}</version>
			</dependency>
			<dependency>
				<groupId>org.awaitility</groupId>
				<artifactId>awaitility</artifactId>
				<version>${version.org.awaitility}</version>
			</dependency>
			<dependency>
				<groupId>org.bouncycastle</groupId>
				<artifactId>bcpkix-jdk18on </artifactId>
				<version>${version.org.bouncycastle}</version>
			</dependency>
			<dependency>
				<groupId>org.eclipse.paho</groupId>
				<artifactId>org.eclipse.paho.client.mqttv3</artifactId>
				<version>${version.org.eclipse.paho}</version>
			</dependency>
			<dependency>
				<groupId>org.keycloak.bom</groupId>
				<artifactId>keycloak-bom-parent</artifactId>
				<version>${version.org.keycloak}</version>
			</dependency>
			<dependency>
				<groupId>org.mapstruct</groupId>
				<artifactId>mapstruct</artifactId>
				<version>${version.org.mapstruct}</version>
			</dependency>
			<dependency>
				<groupId>org.projectlombok</groupId>
				<artifactId>lombok</artifactId>
				<version>${version.org.projectlombok}</version>
			</dependency>

		</dependencies>
	</dependencyManagement>
	<dependencies>

		<!-- avoid compiler warning, see https://docs.micronaut.io/latest/guide/#_nullable_annotations -->
		<!-- TODO mit micronaut 4 entfernen -->
		<dependency>
			<groupId>com.google.code.findbugs</groupId>
			<artifactId>jsr305</artifactId>
			<scope>provided</scope>
		</dependency>

		<!-- build -->
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>

	</dependencies>

	<build>
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-jar-plugin</artifactId>
					<configuration>
						<!-- we do not deploy our jars, so exclude unused maven descriptor -->
						<archive>
							<addMavenDescriptor>false</addMavenDescriptor>
						</archive>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.codehaus.mojo</groupId>
					<artifactId>versions-maven-plugin</artifactId>
					<configuration>
						<ruleSet>
							<rules>
								<rule>
									<groupId>io.micronaut</groupId>
									<artifactId>micronaut-bom</artifactId>
									<ignoreVersions>
										<ignoreVersion>
											<type>regex</type>
											<version>4\..*</version>
										</ignoreVersion>
									</ignoreVersions>
								</rule>
								<rule>
									<groupId>io.micronaut.data</groupId>
									<artifactId>micronaut-data-bom</artifactId>
									<ignoreVersions>
										<ignoreVersion>
											<type>regex</type>
											<version>4\..*</version>
										</ignoreVersion>
									</ignoreVersions>
								</rule>
								<rule>
									<groupId>io.kokuwa.micronaut</groupId>
									<artifactId>micronaut-logging</artifactId>
									<ignoreVersions>
										<ignoreVersion>
											<type>regex</type>
											<version>4\..*</version>
										</ignoreVersion>
									</ignoreVersions>
								</rule>
								<rule>
									<groupId>io.kokuwa.micronaut</groupId>
									<artifactId>micronaut-openapi-codegen</artifactId>
									<ignoreVersions>
										<ignoreVersion>
											<type>regex</type>
											<version>4\..*</version>
										</ignoreVersion>
									</ignoreVersions>
								</rule>
							</rules>
						</ruleSet>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.openapitools</groupId>
					<artifactId>openapi-generator-maven-plugin</artifactId>
					<dependencies>
						<dependency>
							<groupId>io.kokuwa.micronaut</groupId>
							<artifactId>micronaut-openapi-codegen</artifactId>
							<version>${version.io.kokuwa.micronaut.codegen}</version>
						</dependency>
					</dependencies>
				</plugin>
				<plugin>
					<groupId>io.kokuwa.maven</groupId>
					<artifactId>k3s-maven-plugin</artifactId>
					<version>${version.io.kokuwa.maven.k3s}</version>
				</plugin>
			</plugins>
		</pluginManagement>
		<plugins>

			<!-- remove default invocations -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-deploy-plugin</artifactId>
				<executions><execution><id>default-deploy</id><phase /></execution></executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<executions><execution><id>default</id><phase /></execution></executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<executions><execution><id>default</id><phase /></execution></executions>
			</plugin>
			<plugin>
				<groupId>org.codehaus.mojo</groupId>
				<artifactId>flatten-maven-plugin</artifactId>
				<executions><execution><id>default</id><phase /></execution></executions>
			</plugin>
			<plugin>
				<groupId>org.jacoco</groupId>
				<artifactId>jacoco-maven-plugin</artifactId>
				<executions><execution><id>default</id><phase /></execution></executions>
			</plugin>

		</plugins>
	</build>

	<profiles>
		<profile>
			<id>release-prepare</id>
			<build>
				<plugins>
					<plugin>
						<groupId>com.google.code.maven-replacer-plugin</groupId>
						<artifactId>replacer</artifactId>
						<version>1.5.3</version>
						<executions>
							<execution>
								<phase>verify</phase>
								<goals>
									<goal>replace</goal>
								</goals>
								<configuration>
									<file>fluxcd/minoa/prod/kustomization.yaml</file>
									<replacements>
										<replacement>
											<token>newTag: [0-9]+\.[0-9]+\.[0-9]+  # inoa</token>
											<value>newTag: ${project.version}  # inoa</value>
										</replacement>
									</replacements>
								</configuration>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>exec-maven-plugin</artifactId>
						<executions>
							<execution>
								<phase>verify</phase>
								<goals>
									<goal>exec</goal>
								</goals>
								<configuration>
									<executable>git</executable>
									<arguments>
										<argument>add</argument>
										<argument>${project.basedir}/fluxcd/live/prod/kustomization.yaml</argument>
									</arguments>
								</configuration>
							</execution>
						</executions>
					</plugin>
				</plugins>
			</build>
		</profile>
		<profile>
			<id>release-perform</id>
			<properties>
				<image.minoa.tag>${project.version}</image.minoa.tag>
				<image.skipTestImages>true</image.skipTestImages>
			</properties>
		</profile>
	</profiles>
</project>
