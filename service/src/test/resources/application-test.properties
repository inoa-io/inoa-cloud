image.influxdb=${image.influxdb}
image.postgresql=${image.postgresql}

static.path=${project.basedir}/src/test/resources/static

# disable oidc auth, use bearer
micronaut.security.authentication=bearer
