resources: [k3s-full]

patches:
  - target:
      group: apps
      version: v1
      kind: Deployment
      namespace: inoa
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/0/imagePullPolicy
        value: Never
