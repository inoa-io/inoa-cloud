apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: swaggerui
spec:
  rules:
    - host: swaggerui.${k3s.ip}.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: swaggerui
                port:
                  name: http
