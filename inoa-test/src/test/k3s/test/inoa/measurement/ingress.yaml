apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: measurement
spec:
  rules:
    - host: registry.${k3s.ip}.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: measurement
                port:
                  name: http
