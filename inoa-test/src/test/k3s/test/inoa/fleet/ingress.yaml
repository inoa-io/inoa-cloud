apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fleet
spec:
  rules:
    - host: fleet.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: fleet
                port:
                  name: http
