datasources:
  default:
    driverClassName: org.testcontainers.jdbc.ContainerDatabaseDriver
    url: jdbc:tc:postgresql:${docker.postgres.version}:///${datasources.default.database}
    dialect: POSTGRES
