apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: groundcontrol
  annotations:
    kubernetes.io/ingress.class: istio
spec:
  rules:
    - host: dev.inoa.io
      http:
        paths:
          - path: /groundcontrol
            pathType: Prefix
            backend:
              service:
                name: groundcontrol
                port:
                  name: http
  tls:
    - hosts: [dev.inoa.io]
      secretName: tls-inoa-cloud
