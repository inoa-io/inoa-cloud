resources:
  - dashboards-fleet.yaml

configMapGenerator:
  - name: fleet-dashboards
    options:
      disableNameSuffixHash: true
    files:
      - files/gateway-count.json
      - files/inoa-fleet-iot.json
      - files/inoa-fleet-temperature.json
