commonLabels:
  app.kubernetes.io/part-of: inoa
  app.kubernetes.io/name: kafka-ui

resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: kafka-ui-env
    literals:
      - KAFKA_CLUSTERS_0_NAME=inoa
      - KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS=kafka:9092
      - KAFKA_CLUSTERS_0_METRICS_PORT=9090
      - KAFKA_CLUSTERS_0_METRICS_TYPE=PROMETHEUS
