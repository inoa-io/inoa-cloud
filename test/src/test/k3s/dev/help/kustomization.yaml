resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

configMapGenerator:
  - name: help
    files:
      [
        index.html,
        help.html,
        overview.html,
        inoa_bm_color.svg,
        inoa_wm_color.svg,
      ]
