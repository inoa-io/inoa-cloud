apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kafka-console
spec:
  rules:
    - host: kafka.127.0.0.1.nip.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kafka-console
                port:
                  name: http
