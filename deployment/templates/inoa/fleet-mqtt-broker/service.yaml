apiVersion: v1
kind: Service
metadata:
  name: fleet-mqtt-broker
spec:
  type: LoadBalancer
  ports:
    - name: mqtt
      port: 1883
      targetPort: mqtt
    - name: mqtts
      port: 8883
      targetPort: mqtts
  selector:
    app.kubernetes.io/name: fleet-mqtt-broker
